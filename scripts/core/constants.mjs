/**
 * The module id, as declared in `module.json`. Used as the flag scope, the
 * settings namespace, the compendium prefix, and the console log prefix, so it
 * lives on its own with nothing else to import.
 */
export const MODULE_ID = 'nim-plus-package';
