/** Level 1–20, both directions, through the orchestrator — see ./matrix.mjs. */
import { defineMatrix } from './matrix.mjs';

// fixed BUG-migration-classes-a-1 (to203 L7–L20 restores the core Searing Light for a Luminary of Mercy)
defineMatrix('shepherd');
